# 更新日志

格式遵循 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)，
版本号遵循 [语义化版本](https://semver.org/lang/zh-CN/)。

## [1.0.0]

首个版本。

### 功能

- **引用由插件全权决定**：接管 Maisaka `reply` 工具的引用行为，由插件按配置判定
  引用与否。`set_quote` 与 `attach_at` 不再下发给模型（请求前从 `reply` 工具 schema
  中移除），改由插件在响应后无条件写入 —— 模型不参与引用决策，既省 token，
  也不存在「模型省略参数被当成不引用」这类歧义分支。
- **群聊 / 私聊分别配置**：两类会话各有独立的启用开关、固定引用行为、间隔阈值、名单，
  以及**各自的随机权重与「启用随机判定」开关**，互不干扰。权重不存在全局一份，
  每个会话节都完整自包含 —— 改群聊的设置绝不会连带影响私聊。
- **私聊默认不 @**：私聊是一对一说话，@ 对方没有意义，因此 `[private]` 的 `at_weight`
  默认为 `0`；需要时调大即可。
- **软硬双阈值**：
  - `min_message_gap`（软阈值，面板称「引用间隔阈值」）—— 间隔达到该值即引用，
    未达到则交给随机判定；
  - `no_quote_below_gap`（硬阈值，面板称「低于该间隔不引用」）—— 直接结束判定，
    不参与随机。
- **关键词条目**：`[keywords]` 节可按**被回复消息的正文**决定引不引用。
  每条条目自己带「关键词 + 命中后怎么做 + 在哪些会话生效」，
  面板上点「添加」即可新增一条（列表型控件，条目内逐项填写）——
  可选值（`引用` / `不引用`、`不限` / `仅群聊` / `仅私聊`）都是下拉框。
  条目**自上而下匹配，命中第一条即采用其结论、不再往下看（顺序即优先级）**，
  所以把「不引用」的条目排在前面，就等价于"黑名单优先"。
  匹配方式是子串包含、忽略大小写，不写正则也不分词。
  每条还能单独限定群号 / QQ 号，做更细的控制。留空即不启用。
- **随机权重**：每类会话各自带一套权重，在该节的所有条件均无结论时按权重决定回复形态 ——
  引用 / 不引用 二态 + 独立叠加的 @，两次随机。权重为相对值，无需归一化，
  在 WebUI 中以滑条调节。
- **@对方**：通过麦麦原生的 `attach_at` 参数实现真 @（会提醒对方），
  @ 组件由麦麦在自己流程内拼接，**不破坏分段回复与文本润色**。
  需麦麦 `experimental.enable_rich_reply = true`，未开启时自动退化为普通回复。
- **隐藏富回复参数**：`advanced.strip_rich_attachments`（默认开启）会在请求发出前，
  把 `attach_pic` / `attach_emoji` 以及插件接管的 `set_quote` / `attach_at`
  从 `reply` 工具 schema 中移除 —— 模型看不到、也就不会去用，不白耗 token。
  这是麦麦富回复开关的连带能力（三个参数同一开关），插件无法只解锁 `attach_at`，
  因此在 schema 层做减法。
- **配置面板**：每个配置项均带说明；可选值以下拉框呈现，无需记忆取值写法。
- **生效名单 / 排除名单**：支持按群号或用户号精细控制，排除名单优先。
- **命令**：
  - `/引用状态` — 查看当前会话策略、关键词条目、本节的随机权重与判定结果
  - `/引用关键词` — 逐条打印关键词条目（顺序 / 备注名 / 关键词 / 命中后 / 生效会话）
    与群聊 / 私聊策略概览（含各自权重）
- 配置节 `[plugin]` `[group]` `[private]` `[keywords]` `[advanced]`。
- 工具参数的移除只改写**当次请求**的工具表快照，不影响全局工具注册表。

### 说明

- **会话类型与对端 ID 的判定**：麦麦的 `session_id` 是平台、路由与群号/用户号的
  MD5 摘要，字符串里不含 `group` 之类字面量，无法靠关键词分辨。
  插件通过宿主能力 `chat.get_all_streams` 取 `is_group_session`（结果缓存 60 秒），
  并以「上下文里出现 `group_card` 即为群聊」作为确定性兜底；
  对端 ID 同样取自会话表（群聊取 `group_id`、私聊取 `user_id`），
  取不到时返回空串 —— 名单不匹配即保守放行。
- **`enabled = false` 的含义**：不是「完全不干预」，而是「跳过本节的名单与间隔条件，
  改用该会话类型的固定引用行为 / 随机判定」。因为麦麦 `reply` 的缺省是「引用」，
  若真的原样放行，反而会变成「一律引用」。
- **`/引用状态` 的关键词部分**：命令侧拿不到「被回复的那条消息」，没法真的匹配一次，
  因此只报告条目总数与本会话类型适用的条数，并说明该判定需在回复时进行。
- **随机日志的富回复提示**：仅在用户配置了 `at_weight > 0` 且麦麦未开启富回复时提示。
- **`at` 形态的退化**：模型偶尔不带 `msg_id` 调用 `reply`，此时无人可 @，
  `at` 自动降级为普通回复，日志与实际行为保持一致。
- **权重全 0 时的展示**：引用 / 不引用权重全为 0 时，仍会说明 @
  按自身权重独立随机（@ 不受前两项影响）。
- **关键词匹配不到时按未命中处理**：被回复的消息过旧、不在本次上下文里时取不到正文，
  此时关键词条目**不参与判定**，继续走后面的名单与间隔条件 ——
  与间隔条件同样保守：宁可放过，不误判。
